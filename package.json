{
  "name": "xylophone",
  "version": "1.0.0",
  "description": "Sample game for cleo engine.",
  "main": "index.js",
  "scripts": {
    "start:win": "cd dist && tsc && cleo.exe",
    "start:linux": "cd ./dist && tsc && ./cleo",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.2.2"
  }
}
